import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
        light:"#27293D",
        main: "#1E88F7",
        dark: "#1E1E2E",
    },
     type: 'light',
  },
});

export default theme